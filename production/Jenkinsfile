pipeline {
    agent { label 'prod1' }

    stages {
        stage('dockerhub pull') {
            steps {
                sh 'docker pull sivanmarom/test:flask_image-3.0'

            }
        }
    }
}
